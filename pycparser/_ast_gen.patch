Index: pycparser/_ast_gen.py
===================================================================
--- pycparser/_ast_gen.py	(revisión: 91)
+++ pycparser/_ast_gen.py	(copia de trabajo)
@@ -72,13 +72,13 @@
 
         if self.all_entries:
             args = ', '.join(self.all_entries)
-            arglist = '(self, %s, coord=None)' % args
+            arglist = '(self, %s, coord=None, parent=None)' % args
         else:
-            arglist = '(self, coord=None)'
+            arglist = '(self, coord=None, parent=None)'
         
         src += "    def __init__%s:\n" % arglist
         
-        for name in self.all_entries + ['coord']:
+        for name in self.all_entries + ['coord', 'parent']:
             src += "        self.%s = %s\n" % (name, name)
         
         return src
